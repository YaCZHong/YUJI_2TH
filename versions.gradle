ext.build_versions = [:]

build_versions.min_sdk = 21
build_versions.target_sdk = 30
build_versions.compile_sdk = 30
build_versions.build_tools = "30.0.2"

ext.app_version = [:]

app_version.app_version_code = 1
app_version.app_version_name = "1.0.0"


